import 'package:flutter/material.dart';

class PakistanHomeScreen extends StatelessWidget {
  const PakistanHomeScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Pakistan Home Screen Implementation will start soon.'),
    );
  }
}
